const SearchBar = ({busqueda}) => {
  return (
    <>
      <input
        className="p-0 m-0 w-[70%] text-xl lg:text-2xl"
        placeholder="Buscar..."
      />
    </>
  );
};

export default SearchBar;
